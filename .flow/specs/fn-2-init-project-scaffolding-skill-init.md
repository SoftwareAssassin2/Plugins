# init-project scaffolding skill (/init-project)

## Overview

`/init-project` is a **Claude Code skill** (lives at `plugins/init-project/`, registered in `.claude-plugin/marketplace.json`) that stands up a brand-new software project with the user's preferred defaults. It asks only for a **project name** + **short description** (plus a few git/GitHub prompts), then lays down a fixed, opinionated **starter solution** (root layout, root `CLAUDE.md` + `README.md`, dev container, standards docs, a `system.sh` dispatcher CLI, a starter .NET solution, two Angular SPAs, a `postgres` service, and a `keycloak` IdP). It finishes with a **git/GitHub phase** (repo creation, status line, optional initial commit) and then a **terminal opt-in `/dick` (fn-1) hand-off** (since `/dick` is persona-locked until "goodbye", it doesn't reliably return control — the git/commit steps run BEFORE it, and any post-`/dick` action is printed for the user).

The scaffolded project is a **mono-repo containing every component of the software system**, governed by a 1:1 correspondence: **every component is a subfolder `src/<component>/` AND has a matching entry in `config.json`'s top-level `systems[]` array**. External dependencies the system connects to are entries under a peer `services{}` object.

Deterministic file-stamping (skeleton + `__SCAFFOLD_*__` token substitution) is a **bundled, tested shell script** invoked by the skill; per-project adaptation stays in skill prose. The existing `plugins/project-init/*.md` files are raw material — generalized per the **H&G Removal Decisions** section below.

## Starter solution (what gets scaffolded)

Every new project ships these components — each its own `src/<component>/` folder + a `config.json` `systems[]` entry:

**Permanent defaults (always laid down):**
- `Framework` — .NET class library
- `DataAccess` — .NET class library (owns the EF Core `DbContext` + code-first migrations)
- `BusinessLogic` — .NET class library
- `Api` — .NET HTTP API
- `MarketingSite` — Angular SPA (prerendered, client-side only, S3-static, no server runtime)
- `WebApp` — Angular SPA (same build profile)
- `postgres` — Postgres database in a docker container (the database is named `platform`); compose service, OUTSIDE `.devcontainer/` per the dev-container philosophy
- `keycloak` — Keycloak identity provider, self-hosted in a docker container (compose service, OUTSIDE `.devcontainer/`)

The .NET projects compose into a single solution **`src/system.sln`** (layered `Api → BusinessLogic → DataAccess → Framework`). Repo tooling (the `system.sh` dispatcher subcommands) lives under `src/system-cli/`.

**Removable starter sample (clearly marked):**
- `SampleApp` — .NET console app demonstrating the conventions (documented as prunable)

## Quick commands
```bash
jq -e '.plugins[] | select(.name=="init-project")' .claude-plugin/marketplace.json
tmp=$(mktemp -d); ( cd "$tmp" && /path/to/scaffold.sh demo-app "A demo" ) && ls "$tmp/demo-app/src" && ! grep -rqE '__SCAFFOLD_[A-Z0-9_]+__' "$tmp/demo-app" && grep -q '{{' "$tmp/demo-app/config.deploy.json"
# config.json prepopulated with the starter components + services
jq -e '.systems and .services and ([.systems[].name] | index("postgres"))' "$tmp/demo-app/config.json"
# dispatcher distributes config → per-component .env
( cd "$tmp/demo-app" && ./system.sh build-config && ./system.sh help )
```

## Boundaries / non-goals
- NOT opinionated *configuration* of the project's behavior — that is `/dick` (fn-1), the optional last step. (It IS opinionated about default *architecture* — the starter solution above.)
- `postgres` + `keycloak` are **compose-managed runtime components** (`systems[]` entries) and the observability stack is internal tooling — all run as compose stacks OUTSIDE `.devcontainer/`, orchestrated by `system.sh up`. (Distinct from `services{}`, which is external dependencies only, e.g. `claude-api`.)
- Does not **deploy/publish application artifacts** externally; purely local file creation — with one explicit opt-in exception: the final git phase (R15) may create a **GitHub repo** via `gh repo create`.

## Decision context
- **Skill-first, script-behind:** front door is a skill; deterministic stamping delegated to a bundled, tested script (satisfies the 100%-coverage tdd standard, no run-to-run drift).
- **Build-time-complete templates (the core authoring principle):** every template file — `_CLAUDE.md`, `README.md`, `docs/*.md`, `.gitignore`, `.devcontainer/*`, the `system.sh` dispatcher + `src/system-cli/*` subcommands, `config.json`, `config.deploy.json`, compose/realm files — is authored to its **final, complete form during fn-2's `/flow-next:work`**, including the **full verbatim text** of every `_CLAUDE.md` directive specified in this spec. At `/init-project` **runtime the scaffold engine only COPIES these templates and applies minimal `__SCAFFOLD_*__` token substitution** (project name/description; `{{VAR-NAME}}` is reserved for `config.deploy.json` and is NEVER touched by `scaffold.sh`) — it does **NOT** author or LLM-fill prose content. **One bounded exception (deterministic, not authoring):** the engine replaces designated **secret-generation sentinels** with freshly generated URL-safe values (see Secret handling). A freshly scaffolded project is fully-formed from copy-ready templates.
- **Config model:** `config.json` is the single source of truth for local dev — `systems[]` (one entry per `src/<component>/`) + peer `services{}` (one entry per external service, e.g. `claude-api`). `system.sh build-config` distributes per-component `.env`; runtime reads env only (12-factor). A parallel **`config.deploy.json`** is the deployment template — every secret is a `{{VAR-NAME}}` placeholder the CI/CD pipeline substitutes before `build-config` runs.
- **Secret handling:** the `config.json` *template* carries **secret-generation sentinels** (e.g. `__SCAFFOLD_GEN_URLSAFE__`) for the self-hosted local-dev components (`postgres`, `keycloak`); **`scaffold.sh` replaces each `__SCAFFOLD_GEN_URLSAFE__` occurrence with an INDEPENDENTLY generated URL-safe password** (`openssl rand ...` **per occurrence** — every postgres role password, the keycloak admin password, the Api client secret get DISTINCT values, never one shared value) at stamp time — per-project, never committed-static (R23's bounded exception); a test asserts the generated secrets are all distinct + URL-safe. External-service credentials (e.g. `claude-api`) ship as `REPLACE_ME` for the operator. The **generated local-component passwords** (postgres/keycloak — embedded in `.env`/URLs/SQL) must match the **URL-safe alphabet `[A-Za-z0-9_-]+`**, and `build-config` validates **those** (errors on out-of-alphabet). **External-service credentials (e.g. `claude-api`) are EXEMPT** — opaque provider API keys may contain other characters; only fields declared URL-safe are validated.
- **Dispatcher CLI:** a root **`system.sh`** routes `system.sh <subcommand>` → `src/system-cli/<subcommand>.sh`. Subcommands: **`build-config`** (config.json → per-component `.env`, **and stamps the gitignored Keycloak realm-import file from its template, injecting secrets**), **`help`** (lists subcommands + descriptions, each scraped from a `# Description:` comment at the top of its file), **`up`**/**`down`** (bring the per-component compose service stack up/down), **`migrate`** (EF Core migrations), **`status`**. `_`-prefixed files are private helpers (non-dispatchable). (Supersedes the earlier `<project>.sh` / `<name>-cli` idea — the dispatcher is fixed `system.sh` + `src/system-cli/`, not project-named.)
- **Component ↔ folder ↔ config invariant:** each project (incl. .NET class libraries) is its OWN `src/<component>/` + `systems[]` entry; libraries carry minimal/empty config. **Exceptions (NOT `systems[]` components):** `src/system-cli/` (CLI tooling) and the observability tooling under `etc/observability/`; **test projects live under top-level `tests/<Component>.Tests/`, not `src/`** — these exceptions are documented in `_CLAUDE.md`.
- **`main.md` → `_CLAUDE.md`:** the root-CLAUDE template is renamed `_CLAUDE.md` (underscore = template, scaffolds to the project's `CLAUDE.md`).
- **Front-end standard:** Angular SPAs are prerendered (SSG), client-side only, deployed static to an S3 bucket, **no server-side runtime**; **path-based routing** with the S3 error-document→`index.html` fallback (+ CloudFront note) documented in `front-end.md`. Styling **SCSS**; lint/format **angular-eslint + Prettier**; package manager **npm**; unit tests **Jest** (100% coverage applies). Captured in `docs/front-end.md`, linked from `_CLAUDE.md`'s Standards index.
- **Identity provider + DB auth:** Keycloak is the system's IdP, self-hosted (compose). A committed **realm-import JSON** (`--import-realm`) makes local-dev auth reproducible — it pre-defines the realm, SPA public clients (`WebApp`, `MarketingSite`), the `Api` confidential client, a **service-account client per backend service**, and baseline roles; `build-config` injects realm/client ids/secrets from config. **DB access is gated through Keycloak**: the `Api` validates the Keycloak JWT, connects to Postgres as a least-privilege `api` role, and sets per-request **session context** (`app.user_id`/`app.roles` from token claims); **row-level security** policies key off `current_setting('app.*')`. Standards in `docs/keycloak.md`.
- **Database / migrations:** EF Core **code-first** migrations live in the **DataAccess** project; `system.sh migrate` runs **`dotnet ef database update`** (Npgsql) against the connection string built from config. The RLS baseline that can be scaffolded without the schema — `owner`/`migrator`/`api` roles, RLS-on-by-default convention, a session-context helper, and a documented policy template — is authored as **raw SQL inside EF Core migrations**; concrete per-table policies land with the schema.
- **Devcontainer:** base image **`mcr.microsoft.com/devcontainers/dotnet`** + Node feature (for Angular CLI) + **docker-in-docker (or docker-outside-of-docker) + `jq`** — required because `system.sh up/down` drives `docker compose` and `build-config` parses `config.json` with `jq`. The two MCP servers (`context7`, `github-mcp-server`) are configured via a **build-time `templates/.mcp.json`** (NOT written at runtime by setup.sh), coexisting with the `.claude/settings.json` hook/statusline. Toolset: .NET SDK, **Node + Angular CLI (npm)**, GCP CLI, AWS CLI, Azure CLI, GitHub CLI, **Atlassian CLI `acli`** (Jira + Confluence), DuckDB CLI, Claude Code (`install.sh`), **Codex CLI**; enables **nine integrations**: seven **marketplace plugins** (`dick`, `grill-me`, `handoff`, `tdd`, `ubiquitous-language`, Playwright `preferred-browser-automation-plugin`, flow-next `preferred-ralph-loops-plugin`) via `claude plugin`, plus two **MCP servers** (`context7`, `github-mcp-server` — distinct from the `gh` CLI) via `templates/.mcp.json`.
- **CI:** the scaffold ships `.github/workflows/` (GitHub Actions) running .NET + Angular build/test with the **100% line+branch coverage gate** (+ kcov line for shell), matching the tdd standard. Ships an explicit **coverage-exclusion policy** so the gate is satisfiable on a fresh scaffold: generated/bootstrap boilerplate is excluded (EF migrations, ASP.NET `Program`/startup wiring, Angular `main.ts`/bootstrap/`*.config.ts`, generated files) via coverlet filters/`[ExcludeFromCodeCoverage]` + Jest `collectCoverageFrom` excludes; production logic must be covered.
- **Git/GitHub final phase:** after scaffolding the skill (a) prompts — create a GitHub repo? (name?) / auto-commit after setup? / run `/init` after setup?, (b) `git init`s the project, (c) optionally creates the GitHub repo via `gh`, (d) sets up a **status line** (branch + model + % context used), (e) optionally runs `/init`, (f) optionally makes the initial commit (capturing the scaffold + `/init`) **before** the terminal `/dick` hand-off — committing `/dick`'s later doc edits is a printed follow-up command (R7/R15).
- **Docs set (all H&G-free):** the scaffold lays down `docs/` with (a) **empty TODO-stub homes for Dick's business docs** — `business.md`, `strategy.md`, `customers.md`, `priorities.md`, `decisions.md` (owned/filled by `/dick`, fn-1; **no `roadmap.md`** — `priorities.md` replaces it), and (b) the technical standards docs `ubiquitous-language.md`, `architecture.md`, `config-management.md`, `tdd.md`, `dev-container.md`, `front-end.md`, `keycloak.md`. `etc/` holds the observability tooling (fn-2….5) so needs no `.gitkeep`; `src/` is populated by the starter solution. (Dick's `docs/strategy.md`/`docs/decisions.md` are distinct from flow-next's repo-root `STRATEGY.md` / `.flow` decisions.)
- **CLAUDE.md philosophy (hybrid):** inline directives in `_CLAUDE.md` (concision — "sacrifice grammar for concision"; simplicity-over-complexity — Ousterhout/software-entropy; plus the R9 brevity/honesty/verify); deep material in linked docs — **DDD** (Evans) → `ubiquitous-language.md`, **TDD/feedback-loops/deep-modules** (Hunt & Thomas, Ousterhout, "design the interface, delegate the implementation") → `tdd.md`, **design philosophy** → `architecture.md` — surfaced via the Standards index.
- **Auto-update hook:** the scaffolded `.claude/` ships a **non-blocking Stop hook** that surfaces a "refresh `CLAUDE.md`/sub-docs?" reminder **only when** the session surfaced updates worth persisting — **no silent edits**.
- **Doc strategies:** `CLAUDE.md` = thin always-on index (root layout + Standards index + inline directives, depth in `docs/` on demand, hook-maintained); `README.md` = human-facing onboarding (overview, dev-container quickstart, how to run `system.sh` + bring up `postgres`/`keycloak`/observability), distinct from agent-facing CLAUDE.md; **sub-agent convention** = delegate read-heavy exploration/research + parallelizable work to subagents, documented in CLAUDE.md.
- **Business-consult sub-agent (inline `_CLAUDE.md` directive, R20):** before non-trivial work, Claude consults the business direction via a sub-agent that reads the project's `docs/` business docs **in a neutral role** (NOT adopting the `/dick` persona — that can't run in a sub-agent) and reports back — consult-only, with a "flag the gap + suggest interactive `/dick`, never fabricate" fallback.
- **Git-commit policy (inline `_CLAUDE.md` directive, R21):** no commits to `main` without explicit user instruction (ask first if a changeset seems commit-worthy on `main`); commit freely on non-default branches; always `push` after any commit; new branches named `change/<description>` (description only, no epic/spec-id prefix); worktrees created under `.worktrees/`. Mirrors this repo's own operating guidance.
- **Scaffolded `.gitignore` (R22):** ships a stack-aware starting point (.NET + Angular/Node build output, generated `.env` files, OS noise, `.worktrees/`, `.flow/bin/`, local Claude settings); `config.json` stays tracked.
- **Placeholder delimiter (research):** the substitution token must NOT collide with Angular interpolation `{{ }}`, Keycloak realm JS, or EF templating. Use a guarded grammar like `__SCAFFOLD_PROJECT_NAME__` / `__SCAFFOLD_PROJECT_DESCRIPTION__` (the spec's `{{PLACEHOLDER}}` is illustrative); the engine validates no leftover tokens via a `grep -rE '__SCAFFOLD_[A-Z0-9_]+__'` gate.
- **Refuse-by-default on non-empty target**, with two explicit modes: **`--force`** = first-time scaffold into a non-empty dir, allowed only when template paths don't collide with existing **unmanaged** files (refuses on real collisions); **`--update`** = re-scaffold over a prior init-project output, overwriting only `.init-project-manifest.json`-listed paths — but **`config.json` is preserved by default** (operator-edited + holds generated secrets): `--update` structured-merges only NEW non-secret keys and **retains existing generated secrets + operator edits**; rotating secrets/replacing config requires an explicit `--replace-config`. Scaffold into `./<project-name>/`; bash parameter-expansion (no sed-injection).
- **Directory rename:** `plugins/project-init/` → `plugins/init-project/`.

## H&G Removal Decisions

Per-passage rulings from the interview (drive tasks `.2` and `.3`). "H&G" content = the H&G game-catalog domain (game telemetry, Postgres `platform-db`/`platform-api`, Unity, etc.).

| # | Location | Content | Decision |
|---|----------|---------|----------|
| P1 | `main.md:5` | "Shared infrastructure and tooling for H&G's game catalog…" + `play.sh`/`platform/<name>-cli` | **Remove the sentence** entirely |
| P2 | `dev-container.md:24,26` | "services live outside `.devcontainer/`" rule + Postgres/`platform-db`/`platform-api` example | **Keep the principle + dep-placement table; swap the example** to the Grafana+OTel observability stack; strip all H&G |
| P3 | `config-management.md §2 (18-38)` | `play.sh` telemetry keys (`games[]`, ad-campaign, store-listing, package_name, GCS) | **Remove entirely** (no analog in `systems[]`/`services{}`) |
| P4 | `config-management.md §3 (40-61)` | `platform-db` Postgres roles / Flyway / RLS | **Keep generalized**: becomes the real `postgres` component (a `systems[]` entry; database named `platform`); the owner/migrator/api role + RLS pattern is reused (now via EF Core migrations) — remove all H&G/game framing |
| P5 | `config-management.md §4 (63-82)` | `platform-api` Google OAuth / PGS keys | **Replace with a generic `services{}` example** (e.g. `claude-api` → api key/credentials) |
| P6 | `config-management.md §5/§6 (88,103,109,115)` | Incidental Postgres / Cloud SQL / Secret Manager / `platform/` mentions in the password-alphabet + 12-factor sections | **Keep the sections; neutralize the incidental examples** (the URL-safe password alphabet is retained as a real standard — see R24) |
| P7 | `tdd.md (5,14-23,38,46,52,54)` | Unity / `games/` / `coverlet` / `platform-api` / `play.sh` framing | **Keep concept, reword/generalize**: retain 100% line+branch coverage rule, "restructure to test" ethos, Humble-Object idea, tooling-table shape; drop Unity/games/coverlet/play.sh carve-outs; reconcile with the `/tdd` plugin |

> **Override (post-interview):** the earlier instruction in task `.2` to *remove* the `docs/business.md` / `docs/roadmap.md` references is **reversed**: the scaffold creates **empty, H&G-free TODO-stub homes for Dick's business-doc set** (`business.md`, `strategy.md`, `customers.md`, `priorities.md`, `decisions.md`) and indexes them in `_CLAUDE.md`. **`roadmap.md` is dropped** (`priorities.md` replaces it). Owned/filled by `/dick` (fn-1); fn-2 only provides empty homes.
> **Rename (post-interview):** the Postgres component is `src/postgres/` (systems[] entry `postgres`); the *database* it hosts is named `platform`. Earlier "`platform` component" references are superseded.

## Acceptance Criteria
- **R1:** `/init-project` is a registered, invocable skill (`plugins/init-project/SKILL.md` frontmatter `name: init-project` + `argument-hint`; `init-project` entry in marketplace.json with `source: "./plugins/init-project"`).
- **R2:** Invoking the skill asks only for project name + short description, then scaffolds `./<project-name>/` with the root layout (`.claude/ .devcontainer/ docs/ src/ etc/`), root `CLAUDE.md`, standards docs, config-management standard, dispatcher CLI, and dev container — name/description substituted, **no leftover scaffold tokens matching `__SCAFFOLD_[A-Z0-9_]+__`** (the `{{VAR-NAME}}` placeholders in `config.deploy.json` are intentional and exempt).
- **R3:** All scaffolded template content is **generalized per the H&G Removal Decisions** — no "H&G", `play.sh`, Unity/game-framed content (`games[]`, the Unity-`coverlet` carve-out), Google OAuth/PGS, `platform-api`, or `platform-db` references remain. (Coverlet itself is **retained** for .NET coverage per R30 — only the Unity/games framing is removed; the Postgres survives as the generalized `postgres` component per P4.)
- **R4:** The scaffolded **dev container** uses base image `mcr.microsoft.com/devcontainers/dotnet` + Node feature, and installs .NET SDK, **Node + Angular CLI (npm)**, GCP CLI, AWS CLI, Azure CLI, GitHub CLI, Atlassian CLI `acli`, DuckDB CLI, Claude Code, **Codex CLI**; registers this marketplace + enables **nine integrations**: seven **marketplace plugins** (`dick`, `grill-me`, `handoff`, `tdd`, `ubiquitous-language`, Playwright `preferred-browser-automation-plugin`, flow-next `preferred-ralph-loops-plugin`) via `claude plugin`, plus two **MCP servers** (`context7`, GitHub MCP `github-mcp-server`) via `.mcp.json`/settings (NOT `claude plugin install`). All enable/config steps are **best-effort** (isolated warnings; smoke-tested only where the installed CLI supports the command); a fresh build/`setup.sh` succeeds regardless.
- **R5:** Grafana + OTel Collector scaffolded as an **internal observability tooling stack** (a compose stack under `etc/observability/`) outside `.devcontainer/`, launched via `system.sh up`. It is **explicitly outside the `src/<component>` ↔ `systems[]` invariant** — it is dev tooling, NOT a system component and NOT a `services{}` external dependency; `system.sh up`/`down` orchestrate it intentionally (hardcoded), alongside the `postgres` + `keycloak` components.
- **R6:** Bundled scaffold/dispatcher scripts meet the tdd standard — **100% line coverage via kcov + an explicit test case per branch** (kcov branch metrics for bash aren't portable, so branch completeness is enforced by tests, not a kcov branch number); `set -euo pipefail`; source-guarded `main`; refuse non-empty target unless `--force`/`--update` (per the two-mode contract). (.NET/Angular keep 100% line+branch via coverlet/Jest.)
- **R7:** A **terminal** opt-in `/dick` hand-off against the scaffolded project (run AFTER the git phase + optional commit, since `/dick` is persona-locked until "goodbye" and may not return control — the skill never relies on auto-continuation after it; any post-`/dick` step is printed for the user). Graceful non-fatal degradation when `/dick` is absent.
- **R8:** Config-management standard doc + scaffolded `config.json` encode the model: `systems[]` (one entry per `src/<component>/`) + peer `services{}` (one entry per external service), build-step `.env` distribution, 12-factor runtime contract.
- **R9:** Scaffolded root `CLAUDE.md` states mono-repo-of-all-components, documents the `src/<component>` ↔ `systems[]` invariant **and its exceptions** (`src/system-cli/` tooling, `etc/observability/`, `tests/` test projects — not `systems[]` components), and includes behavior directives: brevity/DRY, brutal honesty, always verify before making claims.
- **R10:** The scaffold lays down the **starter solution** — permanent components `Framework`, `DataAccess`, `BusinessLogic`, `Api` (.NET), `MarketingSite`, `WebApp` (Angular), `postgres` (Postgres docker, DB named `platform`), `keycloak` (Keycloak IdP docker), plus a clearly-marked **removable** `SampleApp` (.NET console) — each as `src/<component>/` + a `systems[]` entry.
- **R11:** A `docs/front-end.md` Angular standard ships, specifying: prerendered/SSG, client-side only, S3-static, no server runtime; **path-based routing + the S3 error-document→`index.html` fallback (+ CloudFront note)**; SCSS; angular-eslint + Prettier; npm; Jest unit tests (100% coverage). **Angular major + application-builder versions are pinned**, and acceptance is a smoke build producing static `dist/<app>/browser/index.html` (`outputMode: static` is version-sensitive). The **pinned Angular major + `@angular/build` builder version are declared once** in a single canonical source — the root **`package.json`** (`@angular/*` + `@angular/build` versions) — that R4 (CLI install), R11 (apps), and R30 (CI) all read (no second source), so they cannot drift. **Public runtime config** (realm URL, public client id — non-secret only) reaches each SPA via a `build-config`-stamped gitignored `src/<SPA>/public/config.json` that the app fetches at runtime — **no secrets in `dist/`**. The two Angular SPAs follow it.
- **R12:** The root-CLAUDE template is named `_CLAUDE.md` (scaffolds to `CLAUDE.md`), and its Standards index links `docs/front-end.md`, `docs/keycloak.md`, and the other standards per the documented standards convention.
- **R13:** Scaffolded `config.json` is **prepopulated** with `systems[]` entries for every starter component — incl. the `postgres` settings (host, port, docker, generated URL-safe **passwords** — the database name `platform` and role names `owner`/`migrator`/`api` are FIXED scaffold constants, NOT in user config) and the `keycloak` settings: realm name + **public base URL**, **WebApp + MarketingSite public client IDs** (non-secret), the **Api confidential client ID + generated secret** (with `serviceAccountsEnabled: true` — its service account, NO separate service-account client objects), generated **admin credentials**, host, port, docker — plus the per-SPA **public config fields** (realm URL + public client id) — and a `services{}` example (`claude-api`, `REPLACE_ME`). All field names + env-var names follow the .3 config schema. A parallel `config.deploy.json` mirrors the shape with `{{VAR-NAME}}` placeholders for every secret (R25).
- **R14:** A `docs/keycloak.md` standard ships documenting the Keycloak identity-provider conventions (self-hosted compose, realm-import, DB-auth-via-Keycloak + session-context RLS), linked from `_CLAUDE.md`'s Standards index.
- **R15:** The skill runs a **git/GitHub final phase**: prompts (create GitHub repo? name? auto-commit after setup? run `/init` after setup?), `git init`, optional `gh` repo creation, **status-line setup (branch + model + % context used)** — the statusline script + its `.claude/settings.json` `statusLine` entry are authored as part of the `templates/.claude/` set (fn-2….2, coexisting with the Stop hook); this phase only activates/points at it, never clobbering the hook settings — optional `/init` run, and an optional initial commit — this commit runs **before** the terminal `/dick` hand-off (R7), so it captures the scaffold (and `/init`) output; any commit of `/dick`'s later doc changes is a printed follow-up command. These are **assistant-workflow steps** (a skill instructs the agent; it does not directly execute slash commands) — the skill prompts, then hands off to `/init`/`/dick` where the host supports it, else prints the exact command. All prompts default safe/no-op; a declined repo or absent `gh` degrades gracefully.
- **R16:** The scaffold lays down `docs/` stubs — **empty TODO-stub homes for Dick's business docs** (`business.md`, `strategy.md`, `customers.md`, `priorities.md`, `decisions.md`; owned/filled by `/dick`, fn-1; **no `roadmap.md`**) plus the technical standards docs (`ubiquitous-language.md`, `architecture.md`, `config-management.md`, `tdd.md`, `dev-container.md`, `front-end.md`, `keycloak.md`) — **all H&G-free** — and indexes them in `_CLAUDE.md`. (`etc/` is the catch-all and holds the observability tooling stack (fn-2….5), so no `.gitkeep` is needed.)
- **R17:** `_CLAUDE.md` carries the philosophy content per the hybrid model: **inline** concision directive + simplicity-over-complexity (Ousterhout); **linked** DDD (Evans) → `ubiquitous-language.md`, TDD/feedback-loops/deep-modules → `tdd.md`, design philosophy → `architecture.md`, all surfaced via the Standards index.
- **R18:** The scaffolded `.claude/` ships a **non-blocking Stop hook** that surfaces a "refresh `CLAUDE.md`/sub-docs?" reminder **only when** a **marker file** (e.g. `.claude/.claude-md-dirty`) exists — the assistant writes that marker when it surfaces a doc-worthy update; the Stop hook checks for it, prints the reminder to stdout, and clears it. **No silent edits.** Testable both ways: marker present → reminder shown + cleared; absent → silent.
- **R19:** Doc strategies are realized: `CLAUDE.md` = thin always-on index; `README.md` = human-facing onboarding (overview, dev-container quickstart, run `system.sh` + bring up `postgres`/`keycloak`/observability), distinct from CLAUDE.md; the **sub-agent convention** is documented in `CLAUDE.md`. (`templates/README.md` is authored by fn-2….2.)
- **R20:** `_CLAUDE.md` carries an **inline directive**: **before any non-trivial work**, Claude spawns a sub-agent to **consult the business direction** — read **the project's own** business docs (`docs/business.md`, `strategy.md`, `customers.md`, `priorities.md`, `decisions.md`) **in a neutral consult role** and report back. It **MUST NOT invoke or adopt the `/dick` persona** inside the sub-agent (per `/dick`'s contract, the persona/interview only runs main-thread, never headless); no dependency on any `plugins/dick/` path. **Consult-only**; if the docs can't answer, surface the gap and suggest an interactive `/dick` session — **never fabricating** business direction.
- **R21:** `_CLAUDE.md` carries an inline **git-commit policy**: never commit to `main` without explicit user instruction (ask first if commit-worthy on `main`); commit freely on non-default branches; always `push` after any commit; new branches `change/<description-of-change>` (description only, no epic/spec-id prefix); worktrees under `.worktrees/`. Lives next to the commit instructions.
- **R22:** The scaffold ships a stack-aware **`.gitignore`**: OS noise; `.worktrees/`; .NET (`bin/`,`obj/`,`.vs/`,`*.user`,`[Tt]est[Rr]esults/`); Node/Angular (`node_modules/`,`dist/`,`.angular/`,`coverage/`,`npm-debug.log*`); generated per-component `.env` (NOT `config.json`); the generated runtime Keycloak realm file `src/keycloak/import/*-realm.json`; the generated SPA public config `src/*/public/config.json`; `.claude/settings.local.json`. (Does NOT ignore `.flow/bin/` — flow-next's bundled `flowctl` is a protected pipeline artifact that must stay committable.)
- **R23 (cross-cutting):** Every scaffolded template file is authored to its **final, complete form during fn-2 implementation** — full verbatim directive text lives **in the template files** (not as a pointer to this spec). At `/init-project` runtime the engine performs **only copy + minimal `__SCAFFOLD_*__` token substitution** (+ the bounded secret-gen exception — each `__SCAFFOLD_GEN_URLSAFE__` occurrence independently generated) — no content generation; `{{VAR-NAME}}` is deploy-template-only, never consumed by `scaffold.sh`. A freshly scaffolded project is fully-formed.
- **R24:** A root **`system.sh`** dispatcher routes `system.sh <subcommand>` → `src/system-cli/<subcommand>.sh`, with `_`-prefixed files non-dispatchable. Ships subcommands: **`build-config`** (distributes `config.json` → per-component `.env` + stamps each SPA's public `config.json`; reads its source via `build-config [--config <path>]` defaulting to `config.json`; validates every **field declared URL-safe** (postgres/keycloak credential fields — in ANY concrete config, local `config.json` OR a rendered deploy config) against the alphabet `[A-Za-z0-9_-]+` — external service credentials exempt — AND **structural** configurable fields: filename-safe **realm name**, integer **port** ranges, host/container-name shapes; performs config→file edits via **structured `jq --arg` updates, not string replacement**. The **Postgres role names (`owner`/`migrator`/`api`) and database name (`platform`) are FIXED scaffold constants** — not configurable — so EF migration SQL can reference them literally (no placeholder mechanism); config supplies only host/port + the generated passwords), **`help`** (lists subcommands + a one-line description scraped from each file's top `# Description:` comment), **`up`**/**`down`** (per-component compose stacks), **`migrate`** (EF Core), **`status`**. Carries forward the source repo's dispatcher conventions (exit codes, error format).
- **R25:** Two config files ship: **`config.json`** (local dev — generated URL-safe secrets for `postgres`+`keycloak`, `REPLACE_ME` for external service creds) and **`config.deploy.json`** (deployment template — every secret is a `{{VAR-NAME}}` placeholder). **Deploy handoff:** the CI/CD pipeline renders `config.deploy.json` (substituting `{{VAR-NAME}}` from its secret store) to a concrete file, then runs **`build-config --config <rendered>`** (the subcommand takes `--config`, default `config.json`). Both files share the `systems[]`/`services{}` shape (key-set parity enforced by R30's CI check). **Secret boundary:** the tracked `config.json` intentionally holds **dev-only generated local secrets** (a deliberate trade-off for a single-operator local-dev repo, per the source config-management §6) — these are NOT real/prod secrets; **real secrets are never committed** (they enter only via `config.deploy.json`'s `{{VAR-NAME}}` placeholders rendered at deploy, and the gitignored runtime realm file). **Postgres role-password rotation:** generated role passwords are set by the `postgres` init script at **first volume init only** — rotating them (edit `config.json` + rerun `build-config`) updates `.env` but NOT existing DB roles, so it requires **resetting the postgres volume** (documented) or a future reconciliation step; treat local-dev role passwords as fixed-per-volume.
- **R26:** The .NET projects compose into a single solution **`src/system.sln`** referencing each `src/<component>/` `.csproj`, layered `Api → BusinessLogic → DataAccess → Framework`; `DataAccess` owns the EF Core `DbContext` + migrations.
- **R27:** The committed **`src/keycloak/realm.template.json`** (NOT loaded directly) pre-defines the realm, SPA public clients (`WebApp`, `MarketingSite`), the `Api` confidential client, the `Api`'s **service-account** (client-credentials, for machine-to-machine — the only backend *service* in the starter; `BusinessLogic`/`DataAccess`/`Framework` are libraries, not services), and a baseline role set, with **dev-only dummy secrets pinned** in the committed JSON. `build-config` **stamps a runtime realm-import file (gitignored) from the committed template**, injecting real realm/client ids/secrets from config — never committing real secrets (realm templating is an explicit `build-config` responsibility, R24). The templating is **deterministic JSON-field replacement** (NOT Keycloak env-substitution, which is unreliable): committed template `src/keycloak/realm.template.json` (dummy secrets, NOT in the import mount) → generated `src/keycloak/import/<realm>-realm.json` (gitignored, the only file `--import-realm` loads), replacing client `secret` fields + realm/client ids from `config.json`. **`build-config` owns the import dir deterministically:** it removes any prior generated `*-realm.json` before writing the current one (so a realm rename leaves no stale import file Keycloak would re-import).
- **R28:** DB access is **gated through Keycloak with session-context row-level security**: the `Api` validates the Keycloak JWT, connects to Postgres as a least-privilege `api` login role, and sets per-request session context (`app.user_id`/`app.roles` from claims); RLS policies key off `current_setting('app.*')`. Because `SET LOCAL` only holds inside a transaction (and is pooling-safe), each authenticated request runs inside a **unit-of-work that opens the transaction FIRST, then issues `SET LOCAL`** (an EF transaction step / UoW middleware — NOT a `ConnectionOpened` interceptor, which fires before any `BEGIN`), then runs the request work in that transaction. The schema-independent baseline ships in two layers: (1) the **`owner`/`migrator`/`api` roles (FIXED scaffold constants — not configurable) are bootstrapped by the `postgres` container init script** (before any EF migration — a migration cannot create the role it connects as; fixed names let the static EF migration SQL reference them literally); (2) an initial EF migration — **EF connects as the LOGIN `migrator` role** (a member of the NOLOGIN `owner`; EF cannot connect as a NOLOGIN role), issuing `SET ROLE owner` for owner-privileged DDL — sets `ALTER DEFAULT PRIVILEGES`, installs a **session-context helper**, and ships a **documented per-table RLS policy template** (`ENABLE`+`FORCE ROW LEVEL SECURITY`, `USING`+`WITH CHECK`, keyed off `current_setting('app.user_id', true)`). **Per-table `ENABLE/FORCE` + policies are applied in future entity migrations** — RLS is table-specific and cannot be enabled before tables exist; an optional sample table may demonstrate the pattern.
- **R29:** EF Core **code-first** migrations live in `DataAccess`; **`system.sh migrate`** runs **`dotnet tool restore` then `dotnet ef database update`** (Npgsql) — `dotnet-ef` is pinned in a committed **`.config/dotnet-tools.json`** local tool manifest (the SDK + `Microsoft.EntityFrameworkCore.Design` alone don't provide `dotnet ef`) against the connection string built from the generated config, targeting the `postgres` component's `platform` database (db name + role names are fixed scaffold constants — R24/R28). Migrations run via a **DataAccess `IDesignTimeDbContextFactory`** that reads an **explicitly exported migrator connection string** — `system-cli/migrate.sh` sources the generated config and exports it (.NET does not read `.env` automatically), and the design-time path uses the **`migrator`** role (NOT the runtime `api` connection or `Api` startup auth).
- **R30:** The scaffold ships **CI** under `.github/workflows/` (GitHub Actions) running .NET + Angular build/test with the **100% line+branch coverage gate** (+ kcov for shell), aligned with the tdd standard.
- **R31:** The scaffold ships **CI-based AI code review** on pull/merge requests, turned on by default: a `.github/workflows/` job runs an AI reviewer (**Codex/GPT** — the default backend, reusing the Codex CLI already in the dev container, authed via a CI **secret**) on each **GitHub PR**, posting review feedback; AND the **GitLab** equivalent — a `.gitlab-ci.yml`-style MR-pipeline job that runs **GitLab Duo** code review on each **MR**. **Both** files are scaffolded (each is inert on the other platform, so no platform detection is needed). The review step is **best-effort / non-fatal** (a missing token/secret WARNS, never blocks the pipeline), triggers **only on PRs/MRs** (not every push), and references secrets via CI variables — **never committed** (consistent with R25). Actions/images are pinned (consistent with the repo's pinning convention).

## Early proof point
Task fn-2….1 (skill skeleton + bundled scaffold engine) proves the skill→script→skeleton loop with safe substitution and refuse-non-empty. If it fails, reconsider the skill-first/script-behind split before investing in the starter-solution templates.

## Resolved via Codebase
- Full H&G reference enumeration (grep over `plugins/project-init/`): `main.md:5`; `dev-container.md:24,26`; `config-management.md:18-38,40-61,63-82,88,103,109,115`; `tdd.md:5,14-16,20-23,38,46,52,54`. Governed by the H&G Removal Decisions table.
- No pre-existing front-end.md / Angular preferences anywhere in the repo (grep) — the Angular standard (R11) is authored fresh.
- The `system.sh` dispatcher + `help` `# Description:`-scraping pattern, the `config.json`→`.env` `build-config` step, the URL-safe password alphabet, and the `owner`/`migrator`/`api` + RLS role pattern are all **carried forward from the source repo** (`plugins/project-init/system-cli/system.sh`, `help.sh`, `config-management.md §3/§5`) — generalized off H&G. The interview re-confirmed them as standards.
- `/playwright` is registered as `preferred-browser-automation-plugin` (git-subdir re-export); an existing external plugin, no epic/dependency — joins the devcontainer enabled-plugins list (R4).

## Open Questions
_All four prior open questions were resolved in the fn-2 interview: .NET solution wiring → R26 (`src/system.sln`); Angular routing → R11 (path routing + S3 fallback); auto-update hook trigger → R18 (non-blocking Stop reminder); GitHub plugin identity → R4 (GitHub MCP server)._

Forward notes (not blocking — resolved when the schema/auth model is fleshed out, not at scaffold time):
- **Concrete RLS per-table policies** and the **exact realm role/client model** are scaffolded as baselines + templates now; finalized when real entities exist.

## Strategy drift flagged for review
_None — no STRATEGY.md present._

## Requirement coverage

Every R-ID maps to a concrete task (task ids prefixed `fn-2….`). All 30 are tasked.

| Req | Description | Task(s) |
|-----|-------------|---------|
| R1 | Registered, invocable skill | .1 |
| R2 | Name+description → scaffolded skeleton | .1 |
| R3 | Content generalized per H&G Removal Decisions | .8 |
| R4 | Dev container: base image + CLIs + Node/Angular + 9 plugins (GitHub MCP) | .4 |
| R5 | Grafana + OTel compose via `system.sh up` | .5 |
| R6 | Scripts 100% coverage, refuse-non-empty (scaffold + dispatcher) | .1, .6 |
| R7 | Final opt-in `/dick` hand-off | .7 (depends on fn-1, done) |
| R8 | config.json systems[]/services{} standard | .3 |
| R9 | CLAUDE.md mono-repo + behavior directives | .2 |
| R10 | Starter solution components | .9 (.NET), .10 (postgres/keycloak), .12 (Angular) |
| R11 | front-end.md Angular standard (path routing/S3) + 2 SPAs | .12 |
| R12 | `_CLAUDE.md` + Standards-index links | .2 |
| R13 | config.json + config.deploy.json prepopulated (generated secrets) | .3 |
| R14 | keycloak.md standard (+ DB-auth/RLS) | .8 |
| R15 | Git/GitHub final phase (repo, status line, /init, commit) | .7 |
| R16 | docs/ stubs (Dick biz-docs + tech standards), H&G-free + indexed | .8 |
| R17 | CLAUDE.md philosophy (inline + linked DDD/TDD/design) | .2, .8 |
| R18 | Auto-update Stop hook (reminder, non-blocking) | .2 |
| R19 | Doc strategies (thin CLAUDE.md, human README, subagent convention) | .2 |
| R20 | Inline _CLAUDE.md business-consult sub-agent directive | .2 |
| R21 | Inline _CLAUDE.md git-commit policy (+ branch convention + worktrees) | .2 |
| R22 | Stack-aware `.gitignore` (incl. `.worktrees/`) | .8 |
| R23 | Build-time-complete templates (copy + substitute only) | .1 (engine); all template tasks author complete |
| R24 | `system.sh` dispatcher + `src/system-cli/` subcommands | .6 |
| R25 | config.json + config.deploy.json ({{VAR-NAME}}) + secret-gen + URL-safe alphabet | .3 |
| R26 | Single `src/system.sln` referencing layered .NET projects | .9 |
| R27 | Keycloak realm-import JSON (realm + clients + service accounts) | .10 |
| R28 | DB-auth via Keycloak + session-context RLS baseline (raw SQL in EF migrations) | .11 |
| R29 | EF Core code-first migrations in DataAccess via `system.sh migrate` | .11 |
| R30 | CI: GitHub Actions + 100% coverage gate (+ config drift check) | .13 |
| R31 | CI-based AI code review on PRs (GitHub Codex) + MRs (GitLab Duo) | .14 |

















