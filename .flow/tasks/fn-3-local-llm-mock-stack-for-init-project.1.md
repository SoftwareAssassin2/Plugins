---
satisfies: [R1, R2, R3, R10, R11, R12, R13, R14, R15]
---

## Description
Author the build-time-complete compose templates for the local LLM mock: `etc/local-llm/docker-compose.yml`, the real + mock LiteLLM configs, and a model-pull init script. Prove (manual/integration) both LiteLLM surfaces answer against Ollama under `--profile ai`, and that `--profile ai-mock` answers via `mock_response` with no Ollama container. This is the epic's early proof point.

**Size:** M
**Files** (authored under the **non-default-copied `templates/_optional/local-llm/`** subtree — the `_`-prefix keeps them out of the wholesale `templates/` copy so they never land in a default scaffold; .4 copies `_optional/local-llm/` → `etc/local-llm/` on opt-in): `templates/_optional/local-llm/docker-compose.yml`, `templates/_optional/local-llm/litellm/config.yaml.template` (real, build-config-stamped), `templates/_optional/local-llm/litellm/config.mock.yaml` (mock, static), `templates/_optional/local-llm/_pull-model.sh` (init), unit tests under `templates/tests/system-cli/`, and the **committed integration script `templates/tests/integration/local-llm-mock.sh`** (Docker-gated — skips with a notice when no daemon) plus a Docker-gated job added to the scaffolded CI workflow under `templates/.github/workflows/` (fn-2 R30 — extend the existing workflow that runs `templates/tests/system-cli/`; do not create a parallel one).

## Approach
- Model the stack on the fn-2 observability stack (`etc/observability/docker-compose.yml`) — same `etc/`-tooling category, compose-managed, outside `.devcontainer/`.
- `ollama` service: official `ollama/ollama` **pinned to a version tag** (e.g. `:0.x.y`, NOT `latest` — R14), named volume `/root/.ollama`, **CPU-only by default** (R15; the GPU `deploy.resources.reservations.devices` override is documented in .5, not enabled here), **healthcheck via `ollama list`** (the image has the `ollama` CLI; do NOT assume `curl`), generous `start_period`. Tagged `profiles: ["ai"]`.
- `litellm` service: **PINNED by image digest** (`ghcr.io/berriai/litellm@sha256:…`, optionally with a readable tag alongside — a bare semver tag is re-pushable; capture the exact tested version+digest in this task's Evidence, fn-3….5 documents it), mount config, port 4000, `OLLAMA_API_BASE=http://ollama:11434` env (capability-detection bug). Real instance tagged `profiles: ["ai"]`; a mock instance (mounting `config.mock.yaml`) tagged `profiles: ["ai-mock"]` so it runs WITHOUT Ollama.
- LiteLLM real config: ship committed `litellm/config.yaml.template` with a **wildcard chat route `model_name: "*"` → `ollama_chat/@@LLM_MODEL@@`** (so unmodified app code sending `gpt-4o`/`claude-sonnet-4-6`/any name is served locally — base-URL repoint alone wouldn't route it) plus an explicit `local` alias to the same model (native provider, NOT `openai/` + Ollama `/v1`). `config.mock.yaml` likewise uses a `"*"` wildcard with `mock_response` so any model name is mocked. **`build-config` stamps the gitignored runtime `litellm/config.yaml` from it** by replacing `@@LLM_MODEL@@` with the raw model from `config.json` `localLlm.model` (wiring lands in .2/.4) — mirrors the keycloak realm-stamp pattern. **Embeddings (R12, opt-in):** the template carries a second `model_list` entry `local-embed` → `ollama/@@LLM_EMBED_MODEL@@` (OpenAI `/v1/embeddings` — Anthropic has none) **wrapped in sentinel comment markers** (`# >>> embeddings` … `# <<< embeddings`). `build-config` does a **block operation** (.2): embeddingModel present → keep block, strip markers, substitute `@@LLM_EMBED_MODEL@@`; absent → delete the whole marked block (no orphan entry, no leftover token, valid YAML either way). `config.mock.yaml`: committed static, same chat alias with `mock_response` (no model, no embeddings entry — `ai-mock` is chat-only).
- Anthropic surface: rely on unified `/v1/messages` (auto for all `model_list` entries). Do NOT use the `/anthropic` passthrough.
- Model-pull init: SAME `ollama/ollama` image with an **`entrypoint:` override** (the official image's default entrypoint is `ollama`, so a bare `command: _pull-model.sh` would run `ollama _pull-model.sh`) — e.g. `entrypoint: ["/bin/sh","/scripts/_pull-model.sh"]`. `OLLAMA_HOST=http://ollama:11434`, `restart: "no"`, gated by `depends_on: ollama condition: service_healthy`. The committed **`_pull-model.sh`** must be **POSIX `sh`-compatible** (the entrypoint runs it via `/bin/sh` inside the Ollama image, which may lack Bash) — `set -eu` (NOT `pipefail`/`[[ ]]`/arrays/other bashisms), and its unit tests run under `sh`. (a real script — kcov-coverable) It reads `LLM_MODEL` (+ optional `LLM_EMBED_MODEL`) from its container env (set via compose `${LLM_MODEL:-}` / `${LLM_EMBED_MODEL:-}` interpolation), runs `ollama pull "$LLM_MODEL"` and — when `LLM_EMBED_MODEL` is non-empty (R12) — also `ollama pull "$LLM_EMBED_MODEL"` (one model-pull step, same `ai` profile), and **fails clearly when `LLM_MODEL` is empty**.
- **Pull failure fails loudly (R13):** any `ollama pull` failure (offline / unknown model) makes `_pull-model.sh` exit non-zero; the **real `litellm` waits for the pull** (`depends_on: model-pull condition: service_completed_successfully`), so `up --profile ai` stops with a clear error rather than a half-started stack (the offline/pre-pull workflow is documented in .5).
- All services on a shared network; **LiteLLM published loopback-only `127.0.0.1:4000:4000`** (dev-only unauthenticated gateway — never bind all interfaces; matches the `127.0.0.1` SDK base URLs), Ollama internal-only (no host port).
- Mock LiteLLM (`config.mock.yaml`) must serve canned output through BOTH `/v1/chat/completions` AND `/v1/messages` (the CI path exercises both surfaces).
- Real bind mount of `litellm/config.yaml` uses `create_host_path: false` so a missing generated file errors clearly (no silent dir creation). `up.sh` preflight for `--profile ai` (in .3) requires the generated `litellm/config.yaml` present and consistent with `config.json` (there is no generated `.env` — `LLM_MODEL`/`LLM_EMBED_MODEL` are exported from `config.json`).
- **Two distinct test contexts (resolved in review) — don't conflate:**
  - **Plugin-level (this repo, proving fn-3 works):** unit tests validate the SOURCE `templates/_optional/local-llm/` subtree **unconditionally** (it always exists in the plugin — never skipped); the Docker-gated integration proof **temp-installs `_optional/local-llm/` → a temp `etc/local-llm/` fixture** (or passes a source-path override) and runs `ai-mock` against it, so fn-3's own proof actually exercises the stack (never vacuous).
  - **Scaffolded-project (shipped `templates/tests/...`, run inside a generated project's CI):** these **self-skip with a clear notice (exit 0) when `etc/local-llm/docker-compose.yml` is absent** — so a NON-opt-in generated project's CI is green and the stack stays a non-dependency (R5). The self-skip is for the generated-project path ONLY, never for the plugin's own source validation/proof.
- **No `env_file:` / no generated `.env`** (avoids the `env_file: {required:false}` compose-version dependency): `LLM_MODEL` reaches compose as a `system.sh up`/`down`-exported env var, interpolated as `environment: [LLM_MODEL=${LLM_MODEL:-}]` (.3 owns the export). The real LiteLLM service bind-mounts its generated `litellm/config.yaml` (`volumes:` + `create_host_path: false`), NOT a top-level `configs:` `file:` — so `down` doesn't fail when the generated config is absent. `_pull-model.sh` mounted read-only. `--profile ai` requires `./system.sh build-config` first (stamps `litellm/config.yaml`); document this. Only `config.yaml.template`, `config.mock.yaml`, and `_pull-model.sh` are committed.
- Split evidence: the **unit gate** is generated-file + mocked-shell (no daemon); an **optional Docker-enabled integration job** runs `--profile ai-mock` (no model) and asserts both surfaces **plus that a tools-bearing request is tolerated (no 5xx, well-formed response)** — NOT a structured tool-use round-trip (a canned `mock_response` can't emit `tool_calls`/`tool_use`; structured fidelity is a MANUAL `--profile ai` check). Real-inference `--profile ai` smoke (chat + `/v1/embeddings`) is MANUAL.

## Investigation targets
**Required:**
- `plugins/init-project/templates/etc/observability/docker-compose.yml` — the analog stack to mirror (if authored by fn-2.5; else fn-2 spec R5)
- `plugins/init-project/templates/tests/system-cli/` — existing shell test harness + kcov + stubbing conventions
- fn-2 spec R5 (`etc/` tooling), R23 (build-time-complete), R6 (kcov 100% line + per-branch)

## Key context
- Ollama's OpenAI `/v1` is officially experimental/unreliable for tool calls — route via `ollama_chat`.
- LiteLLM `/v1/messages` streaming has version-specific regressions — pin the image (R11), prefer `mock_response` for tool-use assertions.
- Profiled service is not auto-started by another's `depends_on` unless its profile is active; keep services on a shared network.

## Acceptance
- [ ] `etc/local-llm/docker-compose.yml` defines `ollama` (`ai`) + real `litellm` (`ai`) + mock `litellm` (`ai-mock`) + model-pull init; shared network; LiteLLM :4000, Ollama internal-only; `ollama list` healthcheck + named volume (R1)
- [ ] model-pull init uses `ollama/ollama` with an `entrypoint:` override running `_pull-model.sh` (not `ollama <script>`) + `OLLAMA_HOST` + `ollama pull "$LLM_MODEL"` (from compose `${LLM_MODEL:-}`), gated by `service_healthy`; fails clearly on empty `LLM_MODEL`; no `curl` reliance (R1)
- [ ] `_pull-model.sh` is POSIX `sh`-compatible (`set -eu`, no bashisms — runs via `/bin/sh` in the Ollama image); unit tests run under `sh` (R1, R10)
- [ ] real `litellm` has `depends_on: model-pull condition: service_completed_successfully` (R1, R2)
- [ ] real `config.yaml.template` committed; mock `config.mock.yaml` static (build-config stamps the runtime real config in .2) (R1)
- [ ] `config.yaml.template` shape validated: **wildcard `model_name: "*"`** + explicit `local` alias both → `ollama_chat/@@LLM_MODEL@@` + `api_base: http://ollama:11434`; AND `OLLAMA_API_BASE=http://ollama:11434` on the litellm **compose service env** (not the YAML). `config.mock.yaml` has a `"*"` wildcard with `mock_response`. (Runtime stamping belongs to .2.) (R2)
- [ ] MANUAL/integration smoke (documented, not a CI gate) uses **direct `docker compose -f etc/local-llm/docker-compose.yml --profile ai …` with manually-exported `LLM_MODEL`/`LLM_EMBED_MODEL` + a temp/generated `config.yaml`** — NOT `./system.sh up` (that path + its preflights are .3's scope). Proves both chat surfaces (`/v1/chat/completions` + unified `/v1/messages`) return a completion **when called with a real-looking model name (e.g. `gpt-4o`) — exercising the wildcard route**; AND when an embedding model is set, `/v1/embeddings` returns a vector against `local-embed` (R2, R12)
- [ ] `--profile ai-mock` returns canned `mock_response` output through BOTH `/v1/chat/completions` AND `/v1/messages`, with NO Ollama container running (R3)
- [ ] LiteLLM published `127.0.0.1:4000:4000` (loopback only); Ollama has no host port (R1, R5)
- [ ] LiteLLM image pinned by digest (`@sha256:…`); exact tested version+digest captured in this task's Evidence for fn-3….5 to document (R11)
- [ ] automated generated-file assertion: the LiteLLM image matches `ghcr.io/berriai/litellm(:[^@[:space:]]+)?@sha256:` (digest-only OR tag+digest both pass) and does NOT use `latest`/`main-latest`/a tag-only LiteLLM image (R11)
- [ ] NO `env_file:`/generated `.env`; BOTH `LLM_MODEL` and `LLM_EMBED_MODEL` interpolated via `environment: [LLM_MODEL=${LLM_MODEL:-}, LLM_EMBED_MODEL=${LLM_EMBED_MODEL:-}]`; bind-mounts generated `litellm/config.yaml` (`create_host_path: false`, not `configs:`), so `down` works with `config.yaml` absent; `build-config`-first for `--profile ai` documented (R7, R10, R12)
- [ ] plugin-level: unit tests validate the SOURCE `templates/_optional/local-llm/` subtree UNCONDITIONALLY (no skip — it always exists) (R1, R10)
- [ ] committed integration script `templates/tests/integration/local-llm-mock.sh` (Docker-gated, skips when no daemon; NOT the unit gate): at plugin level it **temp-installs `_optional/local-llm/` → a temp `etc/local-llm/` fixture** and brings it up via **`COMPOSE_PROFILES= docker compose -f <fixture>/docker-compose.yml --profile ai-mock`** (NOT `./system.sh` — .3's scope), asserting canned responses on BOTH surfaces AND that a tools-bearing request is tolerated (no 5xx) — so the proof is never vacuous (R3, R10)
- [ ] scaffolded-project path ONLY: the shipped `templates/tests/...` (incl. the integration script when run inside a generated project) **self-skip (exit 0, clear notice) when `etc/local-llm/docker-compose.yml` is absent** — a non-opt-in generated project's CI is green, stack not a dependency. Self-skip never applies to plugin-level source validation/proof (R5, R10)
- [ ] Ollama image pinned to a version tag (not `latest`); CPU-only default, no GPU reservation in the committed compose (R14, R15)
- [ ] embeddings (R12): `config.yaml.template` carries the marker-wrapped (`# >>> embeddings`/`# <<< embeddings`) `local-embed` → `ollama/@@LLM_EMBED_MODEL@@` block (OpenAI `/v1/embeddings`); `_pull-model.sh` pulls `LLM_EMBED_MODEL` too when set; `ai-mock` has no embeddings entry (R12)
- [ ] pull failure (offline/unknown model) → `_pull-model.sh` exits non-zero → `up --profile ai` fails loudly via `service_completed_successfully` (tested with stubbed `ollama pull` failure) (R13)
- [ ] AUTOMATED CI: generated-file checks + model-pull/any shell at 100% line coverage via kcov, per-branch tests, with ollama/curl/docker stubbed — no daemon/network/model (R10)

## Done summary
_(filled on completion)_

## Evidence
_(filled on completion)_
