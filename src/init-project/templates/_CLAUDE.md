# __SCAFFOLD_PROJECT_NAME__

__SCAFFOLD_PROJECT_DESCRIPTION__

<!-- Placeholder root CLAUDE.md — full content (root layout, Standards index,
     behavior directives, philosophy links, sub-agent + git-commit policy) is
     authored in fn-2 task .2. This stub exists so the scaffold engine (.1) has
     a `_CLAUDE.md` → `CLAUDE.md` mapping to exercise. -->
